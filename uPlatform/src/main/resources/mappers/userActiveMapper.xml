<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.uPlatform.controller.mappers.userActiveMapper">

	<select resultType="UserVO" parameterType="UserVO"
		id="getUserInfo">
		SELECT
			USER_ID
			, USER_PASSWORD
			, USER_EMAIL
			, USER_NAME
			, USER_PHONENUM
			, USER_AUTH
			, USER_BIRTH
		FROM
			USER_INFO
		WHERE
			USER_ID = ${USER_ID}
	</select>
	<insert parameterType="UserVO" id="signUp">
		INSERT INTO
		USER_INFO
			(
			USER_ID
			,	USER_PASSWORD
			,	USER_EMAIL
			,	USER_NAME
			,	USER_PHONENUM
			,	USER_AUTH
			,	USER_BIRTH
			)
		VALUES
			(
			${USER_ID}
			,	${USER_PASSWORD}
			,	${USER_EMAIL}
			,	${USER_NAME}
			,	${USER_PHONENUM}
			,	${USER_AUTH}
			,	${USER_BIRTH}
			)
	</insert>
</mapper>